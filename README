# Tic-Tac-Toe Infinity

Um simples jogo da velha, com a diferença que ele só termina quando alguém ganha.

## Instalação

```cmd
py -m venv env
.\env\Scripts\activate
pip install pygame
deactivate
```

## Execução

```cmd
py main
```
